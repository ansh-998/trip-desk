// Placeholder. Once your Supabase project is live, generate real types with:
//   npx supabase gen types typescript --project-id <your-project-id> > src/types/database.types.ts
// Until then, the hand-written interfaces in src/types/index.ts are the source of truth.
export {}
